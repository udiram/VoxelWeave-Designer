[build]
builder = "DOCKERFILE"
dockerfilePath = "services/release-api/Dockerfile"

[deploy]
startCommand = "node services/release-api/dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
